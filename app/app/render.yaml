services:
  - type: web
    name: minimal-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app -b 0.0.0.0:${PORT}
    plan: free
    envVars:
      - key: APP_ENV
        value: staging
